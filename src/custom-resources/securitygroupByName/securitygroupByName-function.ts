// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for SecuritygroupByNameFunction
 */
export interface SecuritygroupByNameFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/custom-resources/securitygroupByName/securitygroupByName.
 */
export class SecuritygroupByNameFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: SecuritygroupByNameFunctionProps) {
    super(scope, id, {
      description: 'src/custom-resources/securitygroupByName/securitygroupByName.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/custom-resources/securitygroupByName/securitygroupByName.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}