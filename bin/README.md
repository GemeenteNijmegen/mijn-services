# Bin

This is were scripts live.


For example:
- [run migration](./django-migrate/) - Spins up a task and runs the `python src/manage.py migrate` command for the specified migration image (can be configured in the cdk configuration of this project)